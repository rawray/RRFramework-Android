<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary1</item>
        <item name="colorPrimaryDark">@color/primary2</item>
        <item name="colorAccent">@color/primary4</item>
    </style>

    <style name="Theme.ToolBar.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="actionMenuTextColor">@color/white</item>
        <item name="android:background">@color/primary1</item>
    </style>

    <style name="appButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:text">BUTTON</item>
        <item name="android:background">@color/primary3</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="progressbar_loading" parent="android:style/Widget.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/animation_loading</item>
        <item name="android:maxHeight">65dip</item>
        <item name="android:maxWidth">65dip</item>
        <item name="android:minWidth">65dip</item>
        <item name="android:minHeight">65dip</item>
    </style>

</resources>
